# =============================================================================
# ModSecurity Exclusion Rules - Before CRS
# =============================================================================
# Reglas de exclusión que se aplican ANTES de las reglas CRS
# 
# INSTRUCCIONES:
# 1. Copia este archivo sin el .example
# 2. Agrega tus exclusiones personalizadas aquí
# 3. Úsalo para resolver falsos positivos específicos de tu aplicación
#
# =============================================================================

# Ejemplo: Excluir una ruta específica de todas las reglas
# SecRule REQUEST_URI "@beginsWith /api/public" \
#     "id:900001,phase:1,pass,nolog,ctl:ruleEngine=Off"

# Ejemplo: Excluir un parámetro específico de la detección SQL
# SecRule REQUEST_URI "@beginsWith /search" \
#     "id:900002,phase:2,pass,nolog,ctl:ruleRemoveById=942100"

# Ejemplo: Permitir caracteres especiales en un campo específico
# SecRule REQUEST_URI "@beginsWith /api/upload" \
#     "id:900003,phase:2,pass,nolog,ctl:ruleRemoveById=942200"

# NOTA: Descomenta y ajusta estas reglas según tus necesidades específicas

